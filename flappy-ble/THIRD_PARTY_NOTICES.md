# Third-party notices

Blockly core 8.0.0 is retained from the repository's official upstream copy. It is distributed under Apache-2.0 (Google LLC and Blockly contributors). Small UI media are copied from the pinned `blockly@8.0.0` development package. See `LICENSE.blockly.txt` in a CI-built release. No CDN is loaded at runtime.

The course application source and build/test scripts are separate from Blockly. Playwright and TypeScript are development-only tools, not runtime dependencies.
